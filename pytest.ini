[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# Short tracebacks keep CI output readable; -ra summarises non-passes.
addopts = -ra --strict-markers --tb=short

filterwarnings =
    ignore::DeprecationWarning:pandas.*
